import * as React from 'react';
import hiddenIf from '../../../Felles/HiddenIf/hiddenIf';

const Dokumenter: React.FC = () => {
    return <div>Dokumenter</div>;
};

export default hiddenIf(Dokumenter);
